### conda install diagrams
from diagrams import Cluster, Diagram, Edge
from diagrams.custom import Custom
import os
os.environ['PATH'] += os.pathsep + 'C:/Program Files/Graphviz/bin/'

graphattr = {     #https://www.graphviz.org/doc/info/attrs.html
    'fontsize': '22',
}

nodeattr = {   
    'fontsize': '22',
    'bgcolor': 'lightyellow'
}

eventedgeattr = {
    'color': 'red',
    'style': 'dotted'
}
evattr = {
    'color': 'darkgreen',
    'style': 'dotted'
}
with Diagram('sprint3Arch', show=False, outformat='png', graph_attr=graphattr) as diag:
  with Cluster('env'):
     sys = Custom('','./qakicons/system.png')
### see https://renenyffenegger.ch/notes/tools/Graphviz/attributes/label/HTML-like/index
     with Cluster('ctx_sonartest', graph_attr=nodeattr):
          sonar_sim=Custom('sonar_sim','./qakicons/symActorWithobjSmall.png')
     sonar_sim >> Edge( label='sonar_changed', **eventedgeattr, decorate='true', fontcolor='red') >> sys
     sonar_sim >> Edge( label='problem_solved', **eventedgeattr, decorate='true', fontcolor='red') >> sys
diag
